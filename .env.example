# =============================================================================
# MAIA VectorDB — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
#   cp .env.example .env
# =============================================================================

# ---------------------------------------------------------------------------
# REQUIRED
# ---------------------------------------------------------------------------

# PostgreSQL connection string (asyncpg driver for async SQLAlchemy).
# The database must have the pgvector extension installed.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@host.docker.internal:5432/maia_vectors

# OpenAI API key used to generate embeddings.
# The application will refuse to start if this is empty.
OPENAI_API_KEY=sk-your-openai-api-key-here

# Comma-separated list of valid API keys for the X-API-Key header.
# The server will NOT start if this is empty.
API_KEYS=your-secret-key-here

# ---------------------------------------------------------------------------
# OPTIONAL  (defaults shown)
# ---------------------------------------------------------------------------

# Logging level for the application (DEBUG, INFO, WARNING, ERROR, CRITICAL).
# LOG_LEVEL=INFO

# Comma-separated list of allowed CORS origins.
# Defaults to empty (no cross-origin requests permitted).
# Example: CORS_ORIGINS=http://localhost:3000,https://app.example.com
# CORS_ORIGINS=

# Database connection pool size (number of persistent connections).
# DATABASE_POOL_SIZE=5

# Maximum overflow connections above DATABASE_POOL_SIZE (temporary connections
# created when the pool is exhausted).
# DATABASE_MAX_OVERFLOW=10

# Maximum file size for uploads in bytes (default: 10 MB = 10485760).
# Uploads exceeding this limit are rejected with HTTP 413.
# MAX_FILE_SIZE_BYTES=10485760

# OpenAI embedding model name.
# EMBEDDING_MODEL=text-embedding-3-small

# Maximum number of tokens per text chunk.
# CHUNK_SIZE=800

# Number of overlapping tokens between consecutive chunks.
# CHUNK_OVERLAP=200
